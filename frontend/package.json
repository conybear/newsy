{
  "name": "newsy-frontend",
  "version": "1.0.0",
  "description": "Flask-based newspaper frontend",
  "scripts": {
    "start": "echo 'Using Flask app instead of React'",
    "build": "cd .. && ./build.sh",
    "test": "echo 'No tests configured' && exit 0"
  },
  "license": "MIT"
}