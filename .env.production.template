# Acta Diurna - Production Environment Variables Template
# Copy this file and set the actual values for production deployment

# ===================
# BACKEND ENVIRONMENT VARIABLES
# ===================

# MongoDB Connection (Replace with your MongoDB Atlas connection string)
MONGO_URL=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/acta_diurna?retryWrites=true&w=majority

# Database Name
DB_NAME=acta_diurna

# JWT Secret Key (Generate a secure random string)
SECRET_KEY=your-super-secure-secret-key-here

# Environment
ENVIRONMENT=production

# Server Port (Render will set this automatically)
PORT=8001

# ===================
# FRONTEND ENVIRONMENT VARIABLES  
# ===================

# Backend API URL (Replace with your actual backend URL)
REACT_APP_BACKEND_URL=https://acta-diurna-backend.onrender.com/api

# Node Environment
NODE_ENV=production

# Frontend Port (Render will set this automatically)
PORT=3000

# ===================
# INSTRUCTIONS
# ===================

# 1. Create MongoDB Atlas cluster and get connection string
# 2. Generate a secure SECRET_KEY (you can use: openssl rand -hex 32)
# 3. Replace the URLs with your actual Render service URLs
# 4. Set these variables in Render dashboard, not in files
# 5. Never commit actual secrets to version control