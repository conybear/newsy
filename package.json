{
  "name": "acta-diurna",
  "version": "1.0.0",
  "description": "Acta Diurna - Your Personal Daily Chronicle",
  "main": "app.py",
  "scripts": {
    "start": "python app.py",
    "build": "echo '🔨 STARTING ROOT BUILD PROCESS' && echo 'Working directory:' && pwd && echo 'Directory contents:' && ls -la && ./build-production.sh",
    "prebuild": "echo '📋 Pre-build: Ensuring build environment...' && mkdir -p build/static/{js,css,media} && echo '✅ Pre-build environment ready'",
    "postbuild": "echo '🔍 Post-build: Final verification...' && ./debug-build.sh",
    "install": "echo '📥 Installing dependencies...' && cd frontend && yarn install && echo '✅ Dependencies installed'",
    "test": "echo 'Testing Flask application...' && python -c 'import app; print(\"✅ Flask app imports successfully\")' && echo '✅ All tests passed'",
    "verify": "node verify-build.js",
    "debug": "./debug-build.sh"
  },
  "engines": {
    "node": ">=14.0.0",
    "yarn": ">=1.22.0"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {},
  "_deployment": {
    "platform": "emergent",
    "type": "flask-app",
    "build_output": "/app/build",
    "notes": "Creates dummy frontend build for deployment compatibility"
  }
}